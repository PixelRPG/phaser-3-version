{
  "name": "pixelrpg",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "git:submodules": "git submodule update --init --recursive",
    "build": "yarn run build:tile-extruder && yarn run build:esbuild && yarn run build:web-udp && yarn run build:javelin && yarn workspaces foreach run build",
    "build:tile-extruder": "yarn workspace tile-extruder run build && yarn workspace gui run build",
    "build:esbuild": "yarn workspace @pixelrpg/esbuild run build",
    "build:javelin": "yarn workspace javelin run build",
    "build:web-udp": "yarn workspace web-udp run build",
    "build:dev": "yarn workspaces foreach run build:dev",
    "start": "yarn workspace @pixelrpg/tuxemon run start",
    "clear": "yarn workspaces foreach run clear",
    "lint": "yarn workspaces foreach run lint",
    "test": "yarn workspaces foreach run test"
  },
  "files": [
    "src"
  ],
  "author": "Art+Code Studio (Pascal Garber) <pascal@artandcode.studio>",
  "license": "MIT",
  "workspaces": [
    "packages/engine",
    "packages/esbuild",
    "packages/javelin",
    "packages/javelin/packages/*",
    "packages/javelin/examples/*",
    "packages/web-udp",
    "packages/web-udp/packages/*",
    "packages/web-udp/examples/*",
    "packages/eslint-config",
    "packages/tsconfig",
    "packages/tile-extruder",
    "packages/tile-extruder/workspaces/*",
    "examples/*"
  ],
  "devDependencies": {
    "@pixelrpg/eslint-config": "workspace:*",
    "@pixelrpg/tsconfig": "workspace:*",
    "@types/jest": "^27.0.2",
    "@typescript-eslint/eslint-plugin": "^5.1.0",
    "@typescript-eslint/parser": "^5.1.0",
    "@yarnpkg/pnpify": "^3.1.0-rc.2",
    "core-js": "^3.18.3",
    "eslint": "^8.0.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^7.0.4",
    "jest": "^27.3.1",
    "jest-extended": "^1.1.0",
    "postcss-preset-env": "^6.7.0",
    "prettier": "2.4.1",
    "typescript": "^4.4.4"
  },
  "packageManager": "yarn@3.0.2"
}
