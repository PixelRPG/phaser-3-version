import { System } from "@javelin/ecs";

/**
 * Preload game assets
 */
export const assetsSystem: System<any> = (world) => {
  // console.debug("assetsSystem: currentTickData", world.state.currentTickData);
};
