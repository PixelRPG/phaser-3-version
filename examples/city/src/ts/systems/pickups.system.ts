import { System } from "@javelin/ecs";

/**
 * Detect collisions with items and update player inventory
 */
export const pickupsSystem: System<any> = (world) => {
  // console.debug("pickupsSystem: world", world);
};
