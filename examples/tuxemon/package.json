{
  "name": "@pixelrpg/tuxemon",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "build": "yarn run build:assets && yarn run build:esbuild",
    "build:esbuild": "esbuilder",
    "build:assets": "yarn run build:atlas",
    "build:atlas": "free-tex-packer-cli --project ./src/assets/atlas/tuxemon-misa.ftpp",
    "serve:dev": "esbuilder --serve --dev",
    "serve": "esbuilder --serve",
    "start:dev": "yarn run serve:dev",
    "start": "yarn run build:assets && yarn run serve",
    "clear": "rimraf dist",
    "lint": "eslint ./src --ext .js,.jsx,.ts,.tsx --fix && tsc --noEmit",
    "test": "jest"
  },
  "files": [
    "src"
  ],
  "author": "Art+Code Studio (Pascal Garber) <pascal@artandcode.studio>",
  "license": "MIT",
  "devDependencies": {
    "@pixelrpg/esbuild": "workspace:*",
    "@pixelrpg/eslint-config": "workspace:*",
    "@pixelrpg/tsconfig": "workspace:*",
    "@types/jest": "^27.0.2",
    "@typescript-eslint/eslint-plugin": "^5.1.0",
    "@typescript-eslint/parser": "^5.1.0",
    "esbuild": "^0.13.8",
    "eslint": "^8.0.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "free-tex-packer-cli": "^0.3.0",
    "prettier": "2.4.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "@javelin/ecs": "^0.22.0",
    "@pixelrpg/engine": "workspace:*",
    "phaser": "^3.55.2"
  }
}
